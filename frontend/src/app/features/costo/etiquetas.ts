import { TareaCosto } from '../../core/api/costo.model';

/** Etiqueta legible de la tarea del agente que consumió IA. */
export const ETIQUETA_TAREA_COSTO: Record<TareaCosto, string> = {
  scoring: 'Scoring de entrevistas',
  veredicto: 'Veredicto de idea',
};
