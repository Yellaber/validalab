{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "DB_PORT=5432 npm run migration:run:prod",
    "restartPolicyType": "ON_FAILURE"
  }
}
